controller:
  # Docker Desktop Kubernetes exposes LoadBalancer services on localhost
  service:
    type: LoadBalancer
  nodeSelector:
    ingress-ready: "true"
  resources:
    requests:
      cpu: 100m
      memory: 128Mi
    limits:
      cpu: 500m
      memory: 256Mi
