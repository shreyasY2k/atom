# atom-platform-sdk — ATOM's agent SDK (AtomChatModel, HITL, Toolkit)
# Installed automatically by `atom create`; also pulled into the Docker image at build time.
atom-platform-sdk @ git+https://github.com/shreyasY2k/atom.git#subdirectory=atom-sdk/atom_platform_sdk

fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-dotenv>=1.0.0
aiokafka>=0.11.0
