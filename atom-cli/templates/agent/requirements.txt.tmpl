# ATOM's agentscope fork — includes AtomChatModel, ReActAgent, HITL hooks, Toolkit
# The PyPI agentscope package does NOT have AtomChatModel; the fork at atom-sdk/ does.
agentscope @ git+https://github.com/shreyasY2k/atom-sdk.git

fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-dotenv>=1.0.0
aiokafka>=0.11.0
httpx>=0.27.0
