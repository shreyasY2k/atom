package atom.bfsi

# PLACEHOLDER — BFSI compliance policies
#
# This package is reserved for SOC 2, PCI-DSS, GDPR, ISO 27001, and DORA
# guardrail rules. Add rules here when compliance requirements are formalised.
#
# Example rules to add:
#   - Deny LLM calls that include card numbers matching Luhn algorithm (PCI)
#   - Deny requests outside approved geographic regions (GDPR data residency)
#   - Enforce rate limits for regulated operations (DORA resilience)
#   - Require dual-control for transactions above threshold (SOC 2)

default compliant := true
